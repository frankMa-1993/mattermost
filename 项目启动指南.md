# 项目启动指南

本文档用于说明如何在本地手动启动 Mattermost 的前后端开发环境，适合日常开发、联调和排查问题时使用。

## 1. 项目结构

- 根目录：`d:\workspace\mattermost`
- 后端目录：`server/`
- 前端目录：`webapp/`

本项目的典型本地开发方式是：

- 后端服务运行在 `http://localhost:8065`
- 前端开发服务器运行在 `http://localhost:9005`
- 前端开发服务器会把 `/api` 和 WebSocket 请求代理到后端 `8065`

因此，前后端联调时通常访问：

- `http://localhost:9005`

## 2. 启动前准备

首次启动前，建议先确认以下软件已安装：

- Node.js 24
- npm 11
- Go
- Docker Desktop
- GNU Make

Windows 环境额外建议：

- 后端建议在 `Git Bash` 或 `WSL` 中执行 `make` 命令
- 前端在 `PowerShell` 中执行即可
- 如果 PowerShell 阻止执行 `npm.ps1`，请改用 `npm.cmd`

## 3. 推荐启动方式

推荐使用“两终端分开启动”的方式：

- 终端 1：启动后端和数据库依赖
- 终端 2：启动前端开发服务器

这种方式最适合开发前端页面、接口联调和热更新调试。

## 4. 第一次安装依赖

### 4.1 前端依赖

在 `PowerShell` 中执行：

```powershell
cd d:\workspace\mattermost\webapp
npm.cmd install
```

说明：

- `webapp` 是 npm workspaces 根目录
- 执行一次 `npm install` 即可安装前端相关依赖

### 4.2 后端依赖

后端主要依赖：

- Go 运行环境
- Docker 启动 PostgreSQL 等开发依赖服务

通常不需要单独安装额外的 Node 依赖到 `server/` 下。

## 5. 日常启动流程

## 5.1 启动后端

建议在 `Git Bash` 或 `WSL` 中执行：

```bash
cd /d/workspace/mattermost/server
make start-docker
make run-server
```

作用说明：

- `make start-docker`：启动本地开发依赖服务，例如 PostgreSQL
- `make run-server`：启动 Mattermost 后端服务

默认情况下，后端监听地址为：

- `http://localhost:8065`

如果后端正常启动，你通常可以通过浏览器访问：

- `http://localhost:8065`

但在前后端联调时，更推荐最终访问前端开发服务器的 `9005` 端口。

## 5.2 启动前端

建议在 `PowerShell` 中执行：

```powershell
cd d:\workspace\mattermost\webapp
npm.cmd run dev-server --workspace=channels
```

作用说明：

- 启动 `channels` 前端应用的 `webpack-dev-server`
- 默认监听 `http://localhost:9005`
- 自动代理接口请求到 `http://localhost:8065`

启动后访问：

- `http://localhost:9005`

## 5.3 正常联调时的访问方式

当前后端都启动完成后：

- 页面访问：`http://localhost:9005`
- 后端服务：`http://localhost:8065`

通常你只需要打开：

- `http://localhost:9005`

因为：

- 页面资源由前端开发服务器提供
- API 和 WebSocket 会自动转发到后端

## 6. 一条命令启动的方式

如果你想从后端目录一次启动前后端，可以使用：

```bash
cd /d/workspace/mattermost/server
make run
```

说明：

- 该命令会同时启动后端和前端 watch 流程
- 在 Windows 原生环境下，这种方式有时不如分开两个终端稳定
- 日常开发仍然更推荐使用“后端一个终端 + 前端一个终端”的方式

## 7. 前端的两种启动方式

`webapp` 目录下常用的前端启动方式有两种。

### 7.1 开发模式

```powershell
cd d:\workspace\mattermost\webapp
npm.cmd run dev-server --workspace=channels
```

特点：

- 端口是 `9005`
- 支持热更新
- 适合开发前端页面
- 会代理接口到后端 `8065`

这是最推荐的方式。

### 7.2 watch/build 模式

```powershell
cd d:\workspace\mattermost\webapp
npm.cmd run run
```

特点：

- 主要用于 watch 前端构建产物
- 一般配合后端 `8065` 一起使用
- 这种模式下通常直接访问 `http://localhost:8065`

如果你只是开发前端页面，优先使用 `dev-server`。

## 8. 停止服务

### 8.1 停止前端

在前端终端中按：

```text
Ctrl + C
```

### 8.2 停止后端

在后端终端中按：

```text
Ctrl + C
```

### 8.3 停止 Docker 依赖服务

在后端目录执行：

```bash
cd /d/workspace/mattermost/server
make stop-docker
```

## 9. 常见问题

### 9.1 PowerShell 提示不能执行 `npm.ps1`

现象：

- 执行 `npm` 时提示脚本被系统策略阻止

解决方法：

- 不要直接使用 `npm`
- 改用 `npm.cmd`

示例：

```powershell
npm.cmd install
npm.cmd run dev-server --workspace=channels
```

### 9.2 打开 `9005` 但接口报错

排查顺序：

- 确认后端 `8065` 已启动
- 确认数据库容器已启动
- 查看后端终端是否有报错

只启动前端而不启动后端时，页面通常无法正常完成登录和接口调用。

### 9.3 `make` 命令在 PowerShell 中不好用

建议：

- 后端相关命令优先使用 `Git Bash` 或 `WSL`
- 前端相关命令继续在 `PowerShell` 使用即可

## 10. 最常用命令速查

### 后端

```bash
cd /d/workspace/mattermost/server
make start-docker
make run-server
```

### 前端

```powershell
cd d:\workspace\mattermost\webapp
npm.cmd run dev-server --workspace=channels
```

### 访问地址

```text
前端开发页面: http://localhost:9005
后端服务地址: http://localhost:8065
```

## 11. 推荐的日常使用方式

如果你只是做本地开发，建议固定使用下面这套流程：

1. 打开终端 1，进入 `server/`
2. 执行 `make start-docker`
3. 执行 `make run-server`
4. 打开终端 2，进入 `webapp/`
5. 执行 `npm.cmd run dev-server --workspace=channels`
6. 浏览器访问 `http://localhost:9005`

这就是最稳定、最适合日常开发的手动启动方式。

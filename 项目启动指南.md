# 项目启动指南

本文档说明如何在 **Windows** 上本地启动 Mattermost 前后端开发环境。  
**不依赖 GNU Make**，全程可在 PowerShell 中完成。

---

## 最快启动（日常复制粘贴）

按顺序执行，**不要跳过第 1 步**：

### 终端 1：数据库 + 后端

```powershell
# 1. 启动 PostgreSQL 等依赖（必须先做）
cd d:\workspace\mattermost\server
docker compose -f docker-compose.makefile.yml up -d postgres redis inbucket

# 2. 确认 PostgreSQL 已就绪（看到 "accepting connections" 再继续）
docker exec mattermost-postgres pg_isready -U mmuser

# 3. 启动后端
.\bin\mattermost.exe
```

### 终端 2：前端

```powershell
cd d:\workspace\mattermost\webapp
npm.cmd run dev-server --workspace=channels
```

### 浏览器访问

- 开发页面（推荐）：[http://localhost:9005](http://localhost:9005)
- 后端 API：[http://localhost:8065](http://localhost:8065)

---

## 为什么后端总是启动失败？

你遇到的典型报错：

```text
Failed to ping DB ... dial tcp [::1]:5432: connectex: No connection could be made because the target machine actively refused it.
```

**原因：PostgreSQL 没有在运行。**

Mattermost 后端启动时会连接 `localhost:5432` 上的 `mattermost_test` 数据库。  
直接运行 `.\bin\mattermost.exe` **不会**自动启动数据库——数据库由 Docker 容器提供。


| 现象                          | 原因                     | 解决                                      |
| --------------------------- | ---------------------- | --------------------------------------- |
| `5432 ... actively refused` | PostgreSQL 容器未启动       | 先执行 `docker compose ... up -d postgres` |
| `docker: command not found` | Docker Desktop 未安装或未启动 | 打开 Docker Desktop，等托盘图标变绿               |
| `mattermost.exe` 不存在        | 尚未编译后端                 | 见下方「首次编译后端」                             |
| 前端能开但登录/接口报错                | 只启动了前端，后端或数据库未启动       | 确认 8065 端口和后端终端无报错                      |


**与 `make` 无关：** 在 Windows 上装不了 `make` 不会导致上述数据库连接错误。  
`make start-docker` 本质上就是帮你跑 Docker Compose；用上面的 `docker compose` 命令即可替代。

---

## 1. 项目结构

- 根目录：`d:\workspace\mattermost`
- 后端目录：`server/`
- 前端目录：`webapp/`

典型本地开发方式：

- 后端：`http://localhost:8065`
- 前端 dev-server：`http://localhost:9005`（自动把 `/api` 和 WebSocket 代理到 8065）

联调时访问 **[http://localhost:9005](http://localhost:9005)** 即可。

---

## 2. 启动前准备

首次启动前确认已安装：


| 软件                  | 用途                            | 是否必须    |
| ------------------- | ----------------------------- | ------- |
| Docker Desktop      | 运行 PostgreSQL、Redis、Inbucket  | **必须**  |
| Go 1.22+            | 编译/运行后端                       | **必须**  |
| Node.js 24 + npm 11 | 前端开发                          | **必须**  |
| GNU Make            | 仅 Linux/macOS 或 Git Bash 可选流程 | **不需要** |


Windows 额外注意：

- 后端、Docker 命令均在 **PowerShell** 中执行即可
- 前端若遇 `npm.ps1` 被策略阻止，改用 `npm.cmd`
- **每次开机后**先确认 Docker Desktop 已启动，再启动后端

---

## 3. 首次安装

### 3.1 前端依赖

```powershell
cd d:\workspace\mattermost\webapp
npm.cmd install
```

### 3.2 首次编译后端

若 `server\bin\mattermost.exe` 不存在，或你修改了 Go 代码需要重新编译：

```powershell
cd d:\workspace\mattermost\server
go build -o bin/mattermost.exe ./cmd/mattermost
```

> 等价于 `make build`，但不需要 Make。

也可以不编译，直接用 Go 运行（适合改后端代码时）：

```powershell
cd d:\workspace\mattermost\server
go run ./cmd/mattermost
```

### 3.3 首次拉取 Docker 镜像

第一次执行 `docker compose up` 时会自动下载 `postgres:14` 等镜像，可能需要几分钟。

---

## 4. 日常启动流程（推荐）

使用两个终端，分开启动后端和前端。

### 4.1 终端 1：启动依赖 + 后端

**步骤 A — 启动 Docker 依赖（每次开发前必做）**

```powershell
cd d:\workspace\mattermost\server
docker compose -f docker-compose.makefile.yml up -d postgres redis inbucket
```

各服务作用：

- `postgres`：主数据库（**后端启动必需**）
- `redis`：缓存（推荐）
- `inbucket`：本地邮件测试（注册/邀请邮件）

**步骤 B — 确认 PostgreSQL 就绪**

```powershell
docker ps --filter "name=mattermost-postgres"
docker exec mattermost-postgres pg_isready -U mmuser
```

输出应包含 `accepting connections`。若容器状态不是 `healthy`，等待 10–30 秒后重试。

**步骤 C — 启动后端**

```powershell
cd d:\workspace\mattermost\server
.\bin\mattermost.exe
```

看到类似 `Server is listening on [::]:8065` 即表示成功。  
此时可访问 [http://localhost:8065](http://localhost:8065) 验证。

### 4.2 终端 2：启动前端

```powershell
cd d:\workspace\mattermost\webapp
npm.cmd run dev-server --workspace=channels
```

启动后访问 [http://localhost:9005。](http://localhost:9005。)

---

## 5. 停止服务

### 停止前端 / 后端

在对应终端按 `Ctrl + C`。

### 停止 Docker 依赖

```powershell
cd d:\workspace\mattermost\server
docker compose -f docker-compose.makefile.yml stop postgres redis inbucket
```

彻底删除容器和数据卷（**会清空本地数据库**，慎用）：

```powershell
docker compose -f docker-compose.makefile.yml down -v
docker volume rm mattermost-server_postgres-14-data
```

---

## 6. 启动自检清单

后端启动前逐项确认：

```powershell
# Docker 是否在运行
docker info

# 依赖容器是否在跑
docker ps --filter "name=mattermost-"

# PostgreSQL 是否可连接
docker exec mattermost-postgres pg_isready -U mmuser

# 后端二进制是否存在
Test-Path d:\workspace\mattermost\server\bin\mattermost.exe
```

全部通过后，再运行 `.\bin\mattermost.exe`。

---

## 7. 常见问题

### 7.1 `5432 actively refused`（最常见）

PostgreSQL 未启动。按顺序执行：

```powershell
cd d:\workspace\mattermost\server
docker compose -f docker-compose.makefile.yml up -d postgres
docker exec mattermost-postgres pg_isready -U mmuser
.\bin\mattermost.exe
```

### 7.2 Docker Desktop 未运行

报错含 `Cannot connect to the Docker daemon` 或 `pipe ... dockerDesktopLinuxEngine`。

解决：从开始菜单打开 **Docker Desktop**，等状态变为 Running 后重试。

### 7.3 PowerShell 不能执行 `npm.ps1`

改用 `npm.cmd`：

```powershell
npm.cmd install
npm.cmd run dev-server --workspace=channels
```

### 7.4 打开 9005 但接口报错

排查顺序：

1. 后端 8065 是否在运行（浏览器访问 [http://localhost:8065）](http://localhost:8065）)
2. `mattermost-postgres` 容器是否为 `Up (healthy)`
3. 后端终端是否有数据库相关报错

只启动前端而不启动后端/数据库，页面无法正常登录和调接口。

### 7.5 修改 Go 代码后后端没变化

重新编译后再启动：

```powershell
cd d:\workspace\mattermost\server
go build -o bin/mattermost.exe ./cmd/mattermost
.\bin\mattermost.exe
```

或开发时直接用 `go run ./cmd/mattermost`，改代码后 Ctrl+C 再重新运行。

### 7.6 端口被占用

```powershell
# 查看 8065 / 5432 占用
netstat -ano | findstr ":8065"
netstat -ano | findstr ":5432"
```

5432 若被本机其他 PostgreSQL 占用，需先停止该服务，或修改 Docker 端口映射（不推荐新手改）。

---

## 8. 前端的两种启动方式

### 8.1 开发模式（推荐）

```powershell
cd d:\workspace\mattermost\webapp
npm.cmd run dev-server --workspace=channels
```

- 端口 9005，支持热更新
- 自动代理 API 到后端 8065

### 8.2 watch/build 模式

```powershell
cd d:\workspace\mattermost\webapp
npm.cmd run run
```

- 构建产物供后端 8065 直接服务
- 通常访问 [http://localhost:8065](http://localhost:8065)

日常前端开发优先用 **dev-server**。

---

## 9. 可选：有 Make / Git Bash / WSL 时

若环境中有 `make`，可使用项目原生命令（与上面 PowerShell 流程等价）：

```bash
cd /d/workspace/mattermost/server
make start-docker    # 等价于 docker compose up + 等待依赖就绪
make run-server      # 等价于 go run ./cmd/mattermost
```

一条命令同时启前后端：

```bash
make run
```

**Windows 无 Make 时不必安装**，按本文第 4 节流程即可。

---

## 10. 命令速查


| 步骤        | 命令                                                                            |
| --------- | ----------------------------------------------------------------------------- |
| 启动数据库     | `docker compose -f docker-compose.makefile.yml up -d postgres redis inbucket` |
| 检查数据库     | `docker exec mattermost-postgres pg_isready -U mmuser`                        |
| 编译后端      | `go build -o bin/mattermost.exe ./cmd/mattermost`                             |
| 启动后端      | `.\bin\mattermost.exe`                                                        |
| 启动前端      | `npm.cmd run dev-server --workspace=channels`                                 |
| 停止 Docker | `docker compose -f docker-compose.makefile.yml stop`                          |



| 地址              | URL                                            |
| --------------- | ---------------------------------------------- |
| 前端开发页           | [http://localhost:9005](http://localhost:9005) |
| 后端服务            | [http://localhost:8065](http://localhost:8065) |
| 本地邮件 (Inbucket) | [http://localhost:9001](http://localhost:9001) |


---

## 11. 推荐日常流程

1. 打开 Docker Desktop，确认已 Running
2. 终端 1：`docker compose ... up -d postgres redis inbucket` → `pg_isready` → `.\bin\mattermost.exe`
3. 终端 2：`npm.cmd run dev-server --workspace=channels`
4. 浏览器打开 [http://localhost:9005](http://localhost:9005)

这是 Windows 上**最可靠、不依赖 Make** 的手动启动方式。